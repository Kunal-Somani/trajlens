# Performance

## Benchmark

Run the benchmark manually:

```bash
pytest tests/perf/ -m perf -v
```

## Results

Runtime numbers are filled after running on reference hardware post-release. Reference machine specs: fill before v0.4.0 release.
